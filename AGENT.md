# AGENT.md

这个目录是数学学习笔记项目。目标是把 Codex 对话中的重要数学内容整理成可长期复习的 Markdown 笔记。

## 维护方式

- 一个主要分支/课程一个目录，例如 `calculus/`、`abstract_algebra/`、`real_analysis/`、`probability/`。
- 一个小主题尽量一个 Markdown 文件，例如 `calculus/limits.md`、`real_analysis/compactness.md`。
- `README.md` 是总入口；新增分支或重要主题后，应更新链接。
- 每个分支目录可以有自己的 `README.md`，作为该分支目录。
- 一般不需要每个分支都放 `AGENT.md`；除非某个分支有明显不同的写作规则。

## 笔记风格

- 先写直观解释，再写严格公式。
- 公式使用 LaTeX 语法。
- 重要定义、定理、例子、反例、常见误区要保留。
- 对话中的临时闲聊不写入文档，只保留有复习价值的内容。
- 如果一个主题还没讲完，可以在文末加“待补”。

## 文件命名

- 文件名使用英文小写和下划线，例如 `limit_definition.md`。
- 标题可以使用中文，例如 `# 极限的 ε-N 定义`。
- 同一主题后续扩展时，优先更新原文件，不要重复新建相近文件。
